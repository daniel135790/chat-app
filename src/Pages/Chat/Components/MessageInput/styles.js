import { makeStyles } from '@material-ui/core';

const styles = makeStyles({
    sendButtonRoot: {
        borderRadius: '50%'
    },
    messageTextRoot: {
        flex: 1,
        color: '#fff'
    }
});

export default styles;